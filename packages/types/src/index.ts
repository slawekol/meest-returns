// Shared types live here. Domain models (Return, Bag, Refund...) will be
// added alongside the Prisma schema in Step 2 and the API in later steps.

export type Currency = 'PLN' | 'EUR' | 'USD';

export type Iso8601 = string;
